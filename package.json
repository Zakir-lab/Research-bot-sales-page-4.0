{
  "name": "research-bot-sales-page-render-test",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Safe Render backend test for Zakir's existing sales page.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
